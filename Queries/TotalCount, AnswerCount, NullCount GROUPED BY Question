SELECT
    QUESTION_ID,
    QUESTION_NAME,
    COUNT(*) AS TotalCount,
    SUM(CASE WHEN RESULTS_NAME IS NOT NULL THEN 1 ELSE 0 END) AS AnswerCount,
    SUM(CASE WHEN RESULTS_NAME IS NULL THEN 1 ELSE 0 END) AS NullCount
FROM MainMapped
GROUP BY QUESTION_ID, QUESTION_NAME;